{% extends "layout.html.twig" %}

{% block maincontent %}
    {% for message in app.flashes("success") %}
        <div id="alert"><a class="alert-success" href="#alert">{{ message }}</a></div>
    {% endfor %}
    {% for message in app.flashes("error") %}
        <div id="alert"><a class="alert-error" href="#alert">{{ message }}</a></div>
    {% endfor %}
    <div class="pure-g">
        <div class="pure-u-2-3"></div>
        <div class="pure-u-1-3">
            <h4>Date du jour : {{ "now"|date("d/m/Y") }}</h4>
            <h4>Participant(e) : {{ app.user.prenom }} {{ app.user.nom }}</h4>
        </div>

    </div>
    <h5>Filtrer les sorties</h5>
    <form class="pure-form pure-g" method="post">
        <div class="form-row">
            <div class="form-group col-md-6">
        <div class="pure-control-group pure-u-1-3">
            <label for="site">Site :
                <select class="pure-input-1" name="site" id="site">
                    <option value="Tous">Tous</option>
                    {% for site in sites %}
                        <option value="{{ site.nomSite }}">
                            {{ site.nomSite }}
                        </option>
                    {% endfor %}
                </select>
            </label>

            <label for="nom">Le nom de la sortie contient : <input type="search" class="pure-input-1" id="nom"
                                                                   name="nom" onkeyup="rechercheNom()"></label>
            <label for="dateDebut">Entre : <input type="datetime-local" class="searchInput" id="dateDebut"
                                                  name="dateDebut"></label>
            <label for="dateFin">et : <input type="date" class="searchInput" id="dateFin" name="dateFin"></label>

        </div>
            </div>

            <div class="form-group col-md-4">
        <div class="pure-control-group pure-u-1-3">
            <label for="filtres" class="filtres pure-checkbox">Sorties dont je suis l'organisateur/trice<input
                        type="checkbox" id="organisateur" name="filtres"></label>
            <label for="filtres" class="filtres pure-checkbox">Sorties passées<input type="checkbox" id="passees"
                                                                                     name="filtres"></label>
            <label for="filtres" class="filtres pure-checkbox">Sorties auxquelles je suis inscrit/e<input
                        type="checkbox" id="inscrit" name="filtres"></label>
            <label for="filtres" class="filtres pure-checkbox" disabled="">Sorties auxquelles je ne suis pas inscrit/e<input
                        type="checkbox" id="nonInscrit" name="filtres"></label>
        </div>
            </div>

            <div class="form-group col-md-2">
        <div class="pure-control-group pure-u-1-3">
            <input type="submit" class="submitSearch pure-button pure-button-primary" value="Rechercher">
        </div>
            </div>
        </div>
    </form>
{% endblock %}

{% block recherche %}
    <table class="pure-table" id="tab">
        <thead>
        <tr>
            <th>Nom de la sortie</th>
            <th>Date de la sortie</th>
            <th>Clôture</th>
            <th>Inscrits/places</th>
            <th>État</th>
            <th>Inscrit</th>
            <th>Organisateur</th>
            <th>Actions</th>
        </tr>
        </thead>
        <tbody class="fbody" id="sorties">
        {% for sortie in sortiesSansArchivees %}
            {% set compteur = 0 %}
            <tr class="pure-table-odd" id="sortie">
                <td>
                    {{ sortie.nom }}
                </td>
                <td class="dateSortie">
                    {{ sortie.datedebut|date('d/m/Y H\\Hi') }}
                </td>
                <td>
                    {{ sortie.datecloture|date('d/m/Y') }}
                </td>
                <td>
                    {% for participant in sortie.participants %}
                        {% set compteur = compteur + 1 %}
                    {% endfor %}
                    {{ compteur }}/{{ sortie.nbinscriptionsmax }}
                </td>
                <td>
                    {{ sortie.etat }}
                </td>
                <td>
                    {% if app.user in sortie.participants %}
                        <strong>X</strong>
                    {% endif %}
                </td>
                <td>
                    <a href="{{ path('profil_participant', {'id': sortie.organisateur.id}) }}">{{ sortie.organisateur }}</a>
                </td>
                <td>
                    {% if sortie.etat == "Ouverte" and sortie.organisateur != app.user %}
                        <a href="{{ path('sortie_detailSortie', {"id": sortie.id}) }}">Afficher</a> -
                        {% if app.user not in sortie.participants %}
                            <a href="{{ path('sortie_inscription', {"id": sortie.id}) }}">S'inscrire</a>
                        {% elseif app.user in sortie.participants %}
                            <a href="{{ path('sortie_desister', {"id": sortie.id}) }}">Se désister</a>
                        {% endif %}
                    {% endif %}
                    {% if sortie.etat == "Ouverte" and sortie.organisateur == app.user %}
                        <a href="{{ path('sortie_detailSortie', {"id": sortie.id}) }}">Afficher</a> -
                        <a href="{{ path('sortie_annuler', {"id": sortie.id}) }}">Annuler</a>
                    {% endif %}
                    {% if sortie.etat == "Clôturée" %}
                        Afficher
                    {% endif %}
                    {% if sortie.etat == "Activité en cours" %}
                        <a href="{{ path('sortie_detailSortie', {"id": sortie.id}) }}">Afficher</a>
                    {% endif %}
                    {% if sortie.etat == "Créée" and sortie.organisateur == app.user %}
                        <a href="{{ path('sortie_modifierSortie', {"id": sortie.id}) }}">Modifier</a> -
                        Publier
                    {% endif %}
                </td>
                <td style="display: none">
                    {{ sortie.site }}
                </td>
            </tr>
        {% endfor %}
        </tbody>
        <tbody class="fbody" id="mesSorties" style="display: none">
        {% for sortie in mesSorties %}
            {% set compteur = 0 %}
            <tr class="pure-table-odd" id="sortie">
                <td>
                    {{ sortie.nom }}
                </td>
                <td class="dateSortie">
                    {{ sortie.datedebut|date('d/m/Y H\\Hi') }}
                </td>
                <td>
                    {{ sortie.datecloture|date('d/m/Y') }}
                </td>
                <td>
                    {% for participant in sortie.participants %}
                        {% set compteur = compteur + 1 %}
                    {% endfor %}
                    {{ compteur }}/{{ sortie.nbinscriptionsmax }}
                </td>
                <td>
                    {{ sortie.etat }}
                </td>
                <td>
                    {% if app.user in sortie.participants %}
                        <strong>X</strong>
                    {% endif %}
                </td>
                <td>
                    <a href="{{ path('profil_participant', {'id': sortie.organisateur.id}) }}">{{ sortie.organisateur }}</a>
                </td>
                <td>
                    {% if sortie.etat == "Ouverte" and sortie.organisateur != app.user %}
                        <a href="{{ path('sortie_detailSortie', {"id": sortie.id}) }}">Afficher</a> -
                        {% if app.user not in sortie.participants %}
                            <a href="{{ path('sortie_inscription', {"id": sortie.id}) }}">S'inscrire</a>
                        {% elseif app.user in sortie.participants %}
                            <a href="{{ path('sortie_desister', {"id": sortie.id}) }}">Se désister</a>
                        {% endif %}
                    {% endif %}
                    {% if sortie.etat == "Ouverte" and sortie.organisateur == app.user %}
                        <a href="{{ path('sortie_detailSortie', {"id": sortie.id}) }}">Afficher</a> -
                        <a href="{{ path('sortie_annuler', {"id": sortie.id}) }}">Annuler</a>
                    {% endif %}
                    {% if sortie.etat == "Clôturée" %}
                        Afficher
                    {% endif %}
                    {% if sortie.etat == "Activité en cours" %}
                        <a href="{{ path('sortie_detailSortie', {"id": sortie.id}) }}">Afficher</a>
                    {% endif %}
                    {% if sortie.etat == "Créée" and sortie.organisateur == app.user %}
                        <a href="{{ path('sortie_modifierSortie', {"id": sortie.id}) }}">Modifier</a> -
                        Publier
                    {% endif %}
                </td>
                <td style="display: none">
                    {{ sortie.site }}
                </td>
            </tr>
        {% endfor %}
        </tbody>
        <tbody class="fbody" id="sortiesPassees" style="display: none">
        {% for sortie in sortiesPassees %}
            {% set compteur = 0 %}
            <tr class="pure-table-odd" id="sortie">
                <td>
                    {{ sortie.nom }}
                </td>
                <td class="dateSortie">
                    {{ sortie.datedebut|date('d/m/Y H\\Hi') }}
                </td>
                <td>
                    {{ sortie.datecloture|date('d/m/Y') }}
                </td>
                <td>
                    {% for participant in sortie.participants %}
                        {% set compteur = compteur + 1 %}
                    {% endfor %}
                    {{ compteur }}/{{ sortie.nbinscriptionsmax }}
                </td>
                <td>
                    {{ sortie.etat }}
                </td>
                <td>
                    {% if app.user in sortie.participants %}
                        <strong>X</strong>
                    {% endif %}
                </td>
                <td>
                    <a href="{{ path('profil_participant', {'id': sortie.organisateur.id}) }}">{{ sortie.organisateur }}</a>
                </td>
                <td>
                    {% if sortie.etat == "Ouverte" and sortie.organisateur != app.user %}
                        <a href="{{ path('sortie_detailSortie', {"id": sortie.id}) }}">Afficher</a> -
                        {% if app.user not in sortie.participants %}
                            <a href="{{ path('sortie_inscription', {"id": sortie.id}) }}">S'inscrire</a>
                        {% elseif app.user in sortie.participants %}
                            <a href="{{ path('sortie_desister', {"id": sortie.id}) }}">Se désister</a>
                        {% endif %}
                    {% endif %}
                    {% if sortie.etat == "Ouverte" and sortie.organisateur == app.user %}
                        <a href="{{ path('sortie_detailSortie', {"id": sortie.id}) }}">Afficher</a> -
                        <a href="{{ path('sortie_annuler', {"id": sortie.id}) }}">Annuler</a>
                    {% endif %}
                    {% if sortie.etat == "Clôturée" %}
                        <a href="{{ path('sortie_detailSortie', {"id": sortie.id}) }}">Afficher</a>
                    {% endif %}
                    {% if sortie.etat == "Activité en cours" %}
                        <a href="{{ path('sortie_detailSortie', {"id": sortie.id}) }}">Afficher</a>
                    {% endif %}
                    {% if sortie.etat == "Créée" and sortie.organisateur == app.user %}
                        <a href="{{ path('sortie_modifierSortie', {"id": sortie.id}) }}">Modifier</a> -
                        Publier
                    {% endif %}
                </td>
                <td style="display: none">
                    {{ sortie.site }}
                </td>
            </tr>
        {% endfor %}
        </tbody>
        <tbody class="fbody" id="sortiesInscrit" style="display: none">
        {% for sortie in sortiesSansArchivees %}
            {% set compteur = 0 %}
            {% if app.user in sortie.participants %}
            <tr class="pure-table-odd" id="sortie">
                <td>
                    {{ sortie.nom }}
                </td>
                <td class="dateSortie">
                    {{ sortie.datedebut|date('d/m/Y') }}
                </td>
                <td>
                    {{ sortie.datecloture|date('d/m/Y') }}
                </td>
                <td>
                    {% for participant in sortie.participants %}
                        {% set compteur = compteur + 1 %}
                    {% endfor %}
                    {{ compteur }}/{{ sortie.nbinscriptionsmax }}
                </td>
                <td>
                    {{ sortie.etat }}
                </td>
                <td>
                    {% if app.user in sortie.participants %}
                        <strong>X</strong>
                    {% endif %}
                </td>
                <td>
                    <a href="{{ path('profil_participant', {'id': sortie.organisateur.id}) }}">{{ sortie.organisateur }}</a>
                </td>
                <td>
                    {% if sortie.etat == "Ouverte" and sortie.organisateur != app.user %}
                        <a href="{{ path('sortie_detailSortie', {"id": sortie.id}) }}">Afficher</a> -
                        {% if app.user not in sortie.participants %}
                            <a href="{{ path('sortie_inscription', {"id": sortie.id}) }}">S'inscrire</a>
                        {% elseif app.user in sortie.participants %}
                            <a href="{{ path('sortie_desister', {"id": sortie.id}) }}">Se désister</a>
                        {% endif %}
                    {% endif %}
                    {% if sortie.etat == "Ouverte" and sortie.organisateur == app.user %}
                        <a href="{{ path('sortie_detailSortie', {"id": sortie.id}) }}">Afficher</a> -
                        <a href="{{ path('sortie_annuler', {"id": sortie.id}) }}">Annuler</a>
                    {% endif %}
                    {% if sortie.etat == "Clôturée" %}
                        <a href="{{ path('sortie_detailSortie', {"id": sortie.id}) }}">Afficher</a>
                    {% endif %}
                    {% if sortie.etat == "Activité en cours" %}
                        <a href="{{ path('sortie_detailSortie', {"id": sortie.id}) }}">Afficher</a>
                    {% endif %}
                    {% if sortie.etat == "Créée" and sortie.organisateur == app.user %}
                        <a href="{{ path('sortie_modifierSortie', {"id": sortie.id}) }}">Modifier</a> -
                        Publier
                    {% endif %}
                </td>
                <td style="display: none">
                    {{ sortie.site }}
                </td>
            </tr>
            {% endif %}
        {% endfor %}
        </tbody>
        <tbody class="fbody" id="sortiesNonInscrit" style="display: none">
        {% for sortie in sortiesSansArchivees %}
            {% set compteur = 0 %}
            {% if app.user not in sortie.participants %}
            <tr class="pure-table-odd" id="sortie">
                <td>
                    {{ sortie.nom }}
                </td>
                <td class="dateSortie">
                    {{ sortie.datedebut|date('d/m/Y') }}
                </td>
                <td>
                    {{ sortie.datecloture|date('d/m/Y') }}
                </td>
                <td>
                    {% for participant in sortie.participants %}
                        {% set compteur = compteur + 1 %}
                    {% endfor %}
                    {{ compteur }}/{{ sortie.nbinscriptionsmax }}
                </td>
                <td>
                    {{ sortie.etat }}
                </td>
                <td>
                    {% if app.user in sortie.participants %}
                        <strong>X</strong>
                    {% endif %}
                </td>
                <td>
                    <a href="{{ path('profil_participant', {'id': sortie.organisateur.id}) }}">{{ sortie.organisateur }}</a>
                </td>
                <td>
                    {% if sortie.etat == "Ouverte" and sortie.organisateur != app.user %}
                        <a href="{{ path('sortie_detailSortie', {"id": sortie.id}) }}">Afficher</a> -
                        {% if app.user not in sortie.participants %}
                            <a href="{{ path('sortie_inscription', {"id": sortie.id}) }}">S'inscrire</a>
                        {% elseif app.user in sortie.participants %}
                            <a href="{{ path('sortie_desister', {"id": sortie.id}) }}">Se désister</a>
                        {% endif %}
                    {% endif %}
                    {% if sortie.etat == "Ouverte" and sortie.organisateur == app.user %}
                        <a href="{{ path('sortie_detailSortie', {"id": sortie.id}) }}">Afficher</a> -
                        <a href="{{ path('sortie_annuler', {"id": sortie.id}) }}">Annuler</a>
                    {% endif %}
                    {% if sortie.etat == "Clôturée" %}
                        <a href="{{ path('sortie_detailSortie', {"id": sortie.id}) }}">Afficher</a>
                    {% endif %}
                    {% if sortie.etat == "Activité en cours" %}
                        <a href="{{ path('sortie_detailSortie', {"id": sortie.id}) }}">Afficher</a>
                    {% endif %}
                    {% if sortie.etat == "Créée" and sortie.organisateur == app.user %}
                        <a href="{{ path('sortie_modifierSortie', {"id": sortie.id}) }}">Modifier</a> -
                        Publier
                    {% endif %}
                </td>
                <td style="display: none">
                    {{ sortie.site }}
                </td>
            </tr>
            {% endif %}
        {% endfor %}
        </tbody>
        <tbody class="fbody" id="sortiesFiltreDates" style="display: none">
        {% for sortie in sortiesSansArchivees %}
            {% set compteur = 0 %}
            <tr class="pure-table-odd" id="sortie">
                <td>
                    {{ sortie.nom }}
                </td>
                <td class="dateSortie">
                    {{ sortie.datedebut|date('d/m/Y H\\Hi') }}
                </td>
                <td>
                    {{ sortie.datecloture|date('d/m/Y') }}
                </td>
                <td>
                    {% for participant in sortie.participants %}
                        {% set compteur = compteur + 1 %}
                    {% endfor %}
                    {{ compteur }}/{{ sortie.nbinscriptionsmax }}
                </td>
                <td>
                    {{ sortie.etat }}
                </td>
                <td>
                    {% if app.user in sortie.participants %}
                        <strong>X</strong>
                    {% endif %}
                </td>
                <td>
                    <a href="{{ path('profil_participant', {'id': sortie.organisateur.id}) }}">{{ sortie.organisateur }}</a>
                </td>
                <td>
                    {% if sortie.etat == "Ouverte" and sortie.organisateur != app.user %}
                        <a href="{{ path('sortie_detailSortie', {"id": sortie.id}) }}">Afficher</a> -
                        {% if app.user not in sortie.participants %}
                            <a href="{{ path('sortie_inscription', {"id": sortie.id}) }}">S'inscrire</a>
                        {% elseif app.user in sortie.participants %}
                            <a href="{{ path('sortie_desister', {"id": sortie.id}) }}">Se désister</a>
                        {% endif %}
                    {% endif %}
                    {% if sortie.etat == "Ouverte" and sortie.organisateur == app.user %}
                        <a href="{{ path('sortie_detailSortie', {"id": sortie.id}) }}">Afficher</a> -
                        <a href="{{ path('sortie_annuler', {"id": sortie.id}) }}">Annuler</a>
                    {% endif %}
                    {% if sortie.etat == "Clôturée" %}
                        <a href="{{ path('sortie_detailSortie', {"id": sortie.id}) }}">Afficher</a>
                    {% endif %}
                    {% if sortie.etat == "Activité en cours" %}
                        <a href="{{ path('sortie_detailSortie', {"id": sortie.id}) }}">Afficher</a>
                    {% endif %}
                    {% if sortie.etat == "Créée" and sortie.organisateur == app.user %}
                        <a href="{{ path('sortie_modifierSortie', {"id": sortie.id}) }}">Modifier</a> -
                        Publier
                    {% endif %}
                </td>
                <td style="display: none">
                    {{ sortie.site }}
                </td>
            </tr>
        {% endfor %}
        </tbody>
    </table>
    <a href="{{ path('sortie_creerSortie') }}"><input type="submit" class="pure-button pure-button-primary"
                                                      value="Créer une sortie"></a>
{% endblock %}